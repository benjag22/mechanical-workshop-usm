services:
  mysql:
    env_file: .env
    image: mysql:9.4.0
    ports:
      - "33061:3306"

  nginx:
    image: nginx:1.29.2
    ports:
      - "8081:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./images:/var/www/images
